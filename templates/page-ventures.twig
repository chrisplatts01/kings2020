{% extends "base.twig" %}

{% block content %}
	<div class="content-wrapper">
		<article class="post-type-{{post.post_type}}" id="post-{{post.ID}}">
			<section class="article-content">
				<h1 class="article-h1">{{post.title}}</h1>
				<div class="article-body">
					{{post.content}}
				</div>
			</section>
		</article>
		<section id="filters">
			<p>Filters go here</p>
		</section>
		<section id="gallery">
			<p>Ventures gallery goes here</p>
		</section>
	</div><!-- /content-wrapper -->
{% endblock %}
